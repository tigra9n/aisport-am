node_modules/
dist/
.wrangler/
# Scratch tree that scripts/sites-env.sh builds for the sandboxed build:
# an npm cache, a wrangler home and a tmp dir. Machine state, not source.
.sites-runtime/
*.tsbuildinfo
.next/
.DS_Store

# Claude Code's per-machine settings - personal, not shared with the repo
.claude/settings.local.json
