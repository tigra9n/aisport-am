name: Test Content Cron

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Content mode to test'
        required: true
        default: 'rss'
        type: choice
        options: [recap, preview, rss]
  push:
    branches: [main]
    paths: ['.github/workflows/test-modes.yml']

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: |
          echo "=== rss ===" > test.txt
          curl -s --max-time 120 "https://aisport.am/api/cron/content?token=${{ secrets.CRON_TOKEN }}&mode=rss&_c=$(date +%s)v2" >> test.txt 2>&1
        continue-on-error: true
      - run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          # Commit the report onto an orphan tree that holds nothing else.
          # Pushing the whole branch fails the moment a workflow file has
          # changed on it: GitHub refuses to let an Actions token create or
          # update a workflow, and the run is then marked failed even though
          # the work it reports on succeeded. Same fix as add-source.yml.
          git checkout -q --orphan report-tree
          git reset -q
          git add -f test.txt
          git commit -m "check [skip ci]" || true
          git push "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git" HEAD:check-logs --force
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
